@model ServiceDetail

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View</title>
</head>
<body>

    <div>
        <h4>Movie</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Detail.ID)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Detail.ID)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Detail.Description)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Detail.Description)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.Detail.Price)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Detail.Price)
            </dd>


        </dl>

    </div>


    @Html.ListBox("A", new SelectList(Model.ServiceMovies, "MovieCD", "Description"))

    <a asp-action="ServiceMovies"
       class="btn btn-default" >
        >>
    </a>

    @Html.ListBox("A", new SelectList(Model.AviableMovies))


</body>
</html>