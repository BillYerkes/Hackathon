@model IEnumerable<Matriarchy.Models.CompareServices>

@{
    ViewData["Title"] = "Compare Services";
}

<h2>Services</h2>

<table class="table">
    <thead>
        <tr>
            <th>
                Provider
            </th>
            <th>
               Score
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ServiceScore)
                </td>

            </tr>
        }
    </tbody>
</table>

